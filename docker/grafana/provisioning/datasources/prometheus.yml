apiVersion: 1

datasources:
  # This creates a Prometheus data source inside Grafana automatically.
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true